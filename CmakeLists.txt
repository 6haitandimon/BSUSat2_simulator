cmake_minimum_required(VERSION 3.13)

include(pico_sdk_import.cmake)
project(MatherBoard C CXX ASM)

set(CMAKE_C_STANDARD 11)
set(CMAKE_CXX_STANDARD 17)


pico_sdk_init()

#add_subdirectory(PCA9554)
add_subdirectory(MCP2515)
#add_subdirectory(INA3221)
#add_subdirectory(INA219)
add_subdirectory(INTERRUPT)
add_subdirectory(CANTRANSMIT)
add_subdirectory(SX1278)
add_subdirectory(loraPacket)
#add_subdirectory(RTC)

add_link_options(-Wl,-gc-sections,--print-memory-usage,-Map=${PROJECT_BINARY_DIR}/${PROJECT_NAME}.elf.map)
add_executable(MatherBoard
        main.cpp
)

pico_enable_stdio_usb(MatherBoard 1)
pico_enable_stdio_uart(MatherBoard 0)

pico_add_extra_outputs(MatherBoard)
#
#add_custom_command(TARGET MatherBoard POST_BUILD
#        COMMAND ${CMAKE_COMMAND} -E echo "===== Memory Map ====="
#        COMMAND ${CMAKE_COMMAND} -E cat "${CMAKE_BINARY_DIR}/${PROJECT_NAME}.elf.map"
#        COMMAND ${CMAKE_COMMAND} -E echo "=======================")


target_link_libraries(MatherBoard
        pico_stdlib
        MCP2515
        SX1278
        loraPacket
        INTERRUPT
        CANTRANSMIT
)
